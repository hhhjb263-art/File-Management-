# 云匣 CloudVault 正式客户端 · 接口契约（冻结）

> 本文件是 `src/` 各层并行实现的**唯一接口依据**。任何一方需要变更接口，必须先改本文件并通知全体实现方。
> 目标形态：**QML 界面 + 分层 C++**，能力母体为已验证的 `client/`（契约正确），服务端不支持的能力「接口保留、UI 不暴露」。

---

## 0. 分层与依赖规则（禁止跨层反向依赖）

```
qml/  ──调用──▶  controllers/  ──调用──▶  transfer/ · net/ · sync/ · core/
                     │                        ▲
                     └──使用──▶  data/（QML 数据模型）
app/  负责装配：创建 net/transfer/sync + controllers + models，注册到 QML 上下文
```
- QML **只**调用 `controllers/` 与 `data/` 暴露的属性/方法/角色；不直接触碰 `net/`、`transfer/`、`core/`
- `net/`、`transfer/`、`sync/`、`core/` 不 include `controllers/`、`data/`、`qml/`

---

## 1. `net/` —— 服务端真实契约（由 net 实现方负责）

- 抽象接口 `Backend.h` **保持完整**（产品蓝图不删）；服务端不具备的能力返回 **`Unsupported`** 结果，不发请求
- 契约细节以 `server/README.md` + `server/src/app/main.cpp` 的 `server.route(...)` 为准；调用方式对齐 `client/src/MainWindow.cpp`（唯一已验证实现）
- 统一出口负责：URL 拼装 + 百分号编码、`Authorization: Bearer <token>`、HTTP 状态码与 JSON `error` 映射、**TOFU 证书固定**（首次确认→记住 SHA-256→不一致拒绝；弹窗必须 `exec()`）
- 可识别结果：**同名冲突（409）**、缺块（409 missing）、哈希不符（422）、空间不足（507）、未授权（401）、不支持（本地枚举）

**Unsupported 清单**：登录/用户体系、回收站、版本、分享、标签、搜索、parentId 目录体系（服务端用 `dir` 路径字符串）

---

## 2. `transfer/TransferManager` —— 传输队列（冻结 API）

职责：分块上传（含秒传、断点续传、并发 3 块）、Range 下载（4 路窗口、按序落盘、断点）、队列与进度。逻辑移植自 `client/src/MainWindow.cpp` 的已验证实现。

```cpp
class TransferManager : public QObject {
    Q_OBJECT
    Q_PROPERTY(int     activeCount    READ activeCount    NOTIFY changed)
    Q_PROPERTY(QString statusText     READ statusText     NOTIFY changed)  // "… 上传中 3/8"
    Q_PROPERTY(bool    busy           READ busy           NOTIFY changed)

public:
    // 上传：本地文件绝对路径列表 → 远端目录（dir 为相对路径，'' = 根目录）
    Q_INVOKABLE void enqueueUpload(const QStringList &localPaths, const QString &dir);
    // 下载：fileId 列表 → 本地保存目录
    Q_INVOKABLE void enqueueDownload(const QStringList &fileIds, const QString &destDir);
    Q_INVOKABLE void cancelCurrent();

signals:
    void changed();
    void taskProgress(const QString &taskId, qint64 done, qint64 total);   // 进度
    void taskFinished(const QString &taskId, bool ok, const QString &message);
    void conflictDetected(const QString &taskId, const QString &dir, const QString &name);
    // ↑ 同名冲突：由 TransferController 弹"是否覆盖"，再回调解续
    void logMessage(const QString &level, const QString &text);

public slots:
    void resolveConflict(const QString &taskId, bool overwrite);   // 覆盖=true / 跳过=false
};
```

**关键实现约束**（都是踩过的坑，务必保留）
1. 分块上传：`init` → 3 块并发 `PUT chunk`（带 `X-Chunk-SHA256`）→ `complete`；秒传（`done:true`）直接完成
2. **本地 manifest** 记录 `size/mtime/full_hash` 三者一致才续传；`complete` 成功后删除
3. Range 下载：**4 路并发窗口**，乱序到达先缓存、**按序落盘**（写盘串行化，防写坏 `.part`）；`.part` 追加语义保证断点有效
4. 大文件哈希与读块**必须离开 UI 线程**（用 `QThreadPool`/`QtConcurrent`）；工作线程只碰值拷贝，结果经 `QMetaObject::invokeMethod(..., QueuedConnection)` 回主线程
5. 内存与文件大小解耦：绝不整文件读入内存（整文件只走 ≤ 8 MiB 的小文件路径）

---

## 3. `controllers/` —— QML 的唯一 C++ 入口（冻结 API）

| 控制器 | 属性 | 方法（Q_INVOKABLE / slots） | 备注 |
|---|---|---|---|
| `AppController` | `serverUrl` `accessToken` `statusText` `trustSelfSigned` `unsupportedNotice` | `healthCheck()` `refreshStorage()` `saveSettings()` | 已有骨架，补齐真实调用 |
| `FileController` | `currentDir` `loading` `emptyText` | `refresh()` `enterDir(dir)` `createFile(name)` `rename(id,name)` `remove(id)` `createFolder(path)` `uploadHere()` `downloadSelected()` | 列表数据走 `FileListModel` |
| `TransferController` | `activeCount` `statusText` `busy` | `upload(paths,dir)` `download(ids,dest)` `cancel()` `resolveConflict(id,overwrite)` `openLocalFolder()` | 包装 `TransferManager` |
| `StatsController` | `freeBytes` `totalBytes` `freeText` | `refresh()` | 对接 `GET /api/v1/storage` |
| `AuthController` | `supported=false` `userName=""` | — | **服务端不支持**，UI 不暴露入口 |
| `SyncController` | `supported=false` | — | 同上（`sync/` 地基保留，后续启用） |
| `ShareController` | `supported=false` | — | 同上 |

**未支持功能的呈现规则**：`supported=false` 的控制器注册 `unsupportedNotice`（中文说明"服务端暂不支持此功能"）。QML 中对应页面保留但**入口置灰 + tooltip 说明**，不出现可点击后失败的假功能。

---

## 4. `data/` —— QML 数据模型（冻结角色名）

| 模型 | 角色（roleNames） |
|---|---|
| `FileListModel` | `name` `size` `time` `status` `id` `dir`（`id/dir` 供右键菜单使用，界面可不显示） |
| `TransferModel` | `taskId` `fileName` `kind`(upload/download) `done` `total` `progress` `state` `message` |
| 其余（`SyncPairModel`/`ShareLinkModel`/`VersionModel`/`TagModel`） | 保留骨架、`rowCount()==0`，不暴露入口 |

### 4.1 角色语义（**冻结，QML 必须按此绑定**）

`FileListModel`
| 角色 | 类型 | 语义 |
|---|---|---|
| `name` | string | 文件/目录名 |
| `size` | **string（已格式化）** | 展示用，如 `1.23 MB`（对齐母体 `formatSize`） |
| `sizeBytes` | qint64 | **原始字节**（排序/逻辑判断用它） |
| `time` | **string（已格式化）** | 展示用，如 `2026-09-17 08:22`（对齐母体 `formatTime`） |
| `mtime` | qint64 | 原始时间戳 |
| `status` | string | 秒传标记：`是` / `否` / `-`（对齐母体 instantText） |
| `id` | string | 文件 id；目录可用合成 id |
| `dir` | string | 所属目录（相对路径，`""` = 根目录） |
| `hash` | string | 内容哈希（默认不显示） |

> ⚠️ **QML 注意**：`size` 与 `time` 是**字符串**，直接绑定即可显示；需要参与计算/比较时用 `sizeBytes` / `mtime`。

`TransferModel`
| 角色 | 类型 | 语义 |
|---|---|---|
| `taskId` | string | 任务标识 |
| `fileName` | string | 文件名 |
| `kind` | string | `upload` / `download` |
| `done` / `total` | qint64 | 已完成/总字节 |
| `progress` | int | **0..100**（百分比整数） |
| `progressRatio` | double | **0.0..1.0**（`ProgressBar.value` 推荐用它，因为该控件默认区间是 0–1） |
| `state` | string | 逻辑用英文 token：`queued`/`hashing`/`running`/`paused`/`completed`/`failed`/`canceled` |
| `stateText` | string | 显示用中文（如"传输中"） |
| `message` | string | 附加信息/错误文本 |

**排序在模型内完成**（`FileListModel::sortBy(int key, bool asc)`，key 0=名称 1=大小 2=时间；行为已对齐母体 `applySort`），**QML 不做排序**。
**模型只在主线程读写**（网络/IO 结果须经信号回主线程后再改模型），全程无锁。


---

## 5. `qml/` —— 界面文件布局（冻结）

```
qml/Main.qml                     主窗口：head 工具栏 + 内容区 + 状态条
qml/theme/Theme.qml              Singleton 设计令牌（色彩/字号/间距/圆角），对应 docs/UI优化方案.md §2.1
qml/controls/                    复用控件：PrimaryButton / SecondaryButton / GhostButton / DangerButton /
                                 StatusBadge / EmptyState / SectionCard / SearchField
qml/pages/FilesPage.qml          文件列表（含空态、加载态、右键菜单）
qml/pages/TransfersPage.qml      传输队列（进度、取消、冲突处理）
qml/pages/SettingsPage.qml       连接设置（地址/令牌/证书指纹查看清除）
qml/pages/{SharesPage,TagsPage,TrashPage,VersionsPage}.qml   置灰占位页（"服务端暂不支持"）
qml/dialogs/                     新建文件 / 重命名 / 删除确认 / 覆盖询问 / 证书指纹确认 / 关于
```
**响应式三档（`Theme.qml` 中集中定义断点）**：`≥1280` 三栏（列表+预览+传输）· `768–1280` 预览变抽屉 · `<768` 单栏 + 底部标签页。

---

## 6. 验证方式（所有实现方统一）

```bash
cd /d/Qtproject/File/build_official
export PATH="/d/Qt/6.9.3/mingw_64/bin:/d/Qt/Tools/mingw1310_64/bin:$PATH"
mingw32-make -f Makefile.Release ../build/obj/<你的文件>.o     # 逐文件
# 全量（等所有缺口补齐后）：
/d/Qt/6.9.3/mingw_64/bin/qmake.exe ../File.pro && mingw32-make -j4
```
- 要求：**RC=0、无 error**；沙箱偶发拦截编译器（RC≠0 且 stderr 全空）→ 重试
- 服务端是 POSIX，本机跑不起来 → 端到端联调需用户在 Debian 上执行

---

## 7. 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-09-17 | 首次冻结 | 三路并行实现前统一接口 |
| 2026-09-17 | 追加「8. 决策记录（lead 裁决）」 | net 实现方提出 2 个语义冲突点 |
| 2026-09-18 | 追加 §8.7「传输队列历史分组 + 自动刷新」（加法式） | 用户需求：队列任务完成后归入「已下载/已上传」（默认隐藏）、文件列表自动刷新 |
| 2026-09-18 | 追加 §8.8：`FileController::refreshFinished(bool)` 信号（加法式）；`TransferModel` 补齐「同 id 不同时存在于活动表与历史」不变量 | 修复 QA 证实的 P0：「自动刷新失败即暂停」为死路径（statusMessage 从不在失败/成功路径发出）；P2：同 id 可同时存在于两处 |

---

## 8. 决策记录（lead 裁决，全体实现方遵守）

### 8.1 删除语义：`purge()` = 真实永久删除；回收站一族 = unsupported
- **裁决**：`purge(id)` **映射到真实接口 `DELETE /api/v1/files/{id}`**（服务端该接口即"删除并回收空间"，返回 `freed_bytes`）；`moveToTrash`/`restore`/`listTrash`/`emptyTrash` 一律 `unsupported`（服务端无回收站）。
- **理由**：若严格按字面把 `purge` 也列为 unsupported，正式客户端将**没有任何删除路径**，与产品能力不符。服务端语义是"硬删除"，故用 `purge` 承载它最贴切。
- **UI 影响**：文件右键"删除"= 真实删除；文案必须明确「删除后不可恢复」（因为服务端没有回收站）。

### 8.2 分块上传的"同名覆盖"：新增**加法式**接口（不改既有签名）
- **问题**：`Backend.h::beginUpload(parentId,name,size,sha256)` 不携带 `overwrite`，导致分块上传只能把 409 当错误、无法覆盖。
- **裁决**：在 `Backend.h` **新增一个带默认实现的虚函数**（对既有实现零破坏）：
  ```cpp
  // 上传同名文件时是否覆盖（仅对"整文件上传"与"分块 init"生效）。默认 false。
  virtual void setUploadOverwrite(bool enabled) { (void)enabled; }
  ```
  - `HttpBackend` 存储该标志：① 整文件上传 → 头 `X-CV-Overwrite: 1`；② 分块 `init` body → `"overwrite": true`
  - `MockBackend` 继承默认实现（no-op），**不需要改**
- **调用方约定（`TransferManager`）**：收到 `conflictDetected` → 上层 `resolveConflict(taskId, true)` 时，先 `backend->setUploadOverwrite(true)`，其余情况保持 `false`；一次覆盖只对随后的那一次上传生效，用后立即复位。

### 8.3 TOFU 确认框：net 层不弹窗，由界面注入回调（QML）
- `src/net/HttpBackend` **不依赖 QtWidgets**（正式客户端链接里没有 Qt6Widgets）。它暴露 `setTrustPrompt(回调)` 由界面层注入确认逻辑；**未注入回调时一律 fail-closed（拒绝连接）**，绝不静默信任。
- **唯一注入者 = `AppController`**：它在构造时调用 `installTrustPrompt()` 注入回调，并通过 `trustPromptRequested` / `answerTrustPrompt(bool)` 与 QML 交互（内部有 `receivers()==0` 守卫，无人接线即拒绝）。
- ⚠️ **`HttpBackend::setTrustPrompt()` 是「赋值」语义，不是追加** —— 全工程**只允许一个注入者**。装配层（`app/Application`）**不得**再次注入，否则后注入者会覆盖前者，导致 QML 只连到其中一条、另一条成为死路径（此缺陷已发生过一次：曾出现"弹窗永不出现 + fail-closed 拒绝连接"）。
- **QML 侧义务**：`CertPinDialog` 绑定 **`App.trustPromptRequested`**，回答时调 **`App.answerTrustPrompt(bool)`**——首次连接展示 `host:port` + 分组指纹(SHA-256) + subject/issuer/有效期；选"信任"→ 写入 `pinnedFingerprint/<host:port>` 并放行；选"不信任"→ 拒绝；**指纹不一致 → 警告且不提供"继续"**。不要再连 `Core.*`（`Application` 的同名 API 已删除）。
- 指纹持久化文件与 `client/` **不同**（正式客户端用 `AppPaths::settingsFile()`），两者不共享信任记录——这是预期行为，文档需说明。

### 8.4 覆盖同名文件**必须原子**：禁止"先删再传"
- **问题**：`TransferManager` 初版把"覆盖"实现为 `purge(file_id)` 删除旧文件 → 再重新 `init` 上传。这是**两步非原子操作**：若中途失败/断电，用户会**永久丢失原文件**（服务端无回收站，删了就没了）。
- **裁决**：覆盖一律走**服务端原子覆盖语义**：
  - 整文件上传：请求头 `X-CV-Overwrite: 1`
  - 分块上传：`init` body 带 `"overwrite": true`（会话在 `complete` 时替换同名记录内容，失败时旧文件保持不变）
  - 触发方式：`backend->setUploadOverwrite(true)` → 重新发起该次上传 → **发起后立即复位为 false**
- **禁止**：任何形式的"先删旧文件再传新文件"覆盖实现。

### 8.5 分块大小：**单一来源**，由后端暴露
- **问题**：`TransferManager` 用常量 5 MiB 计算 offset→seq，而 `HttpBackend` 从 `Settings::chunkSizeMB()` 取块大小（可被用户改动）。两者一旦不一致，`putChunk` 会被服务端判 400（seq 与 offset 不匹配）。
- **裁决**：在 `Backend.h` 新增（带默认实现，加法式）：
  ```cpp
  virtual qint64 chunkSize() const { return 5 * 1024 * 1024; }   // 分块上传的块大小（字节），唯一来源
  ```
  - `HttpBackend` 覆盖它，返回**它 init 时实际使用**的块大小；并且 **init 直接用该返回值**，不再从 `Settings::chunkSizeMB()` 读取（避免留下一个会让两端失配的隐藏旋钮）
  - `TransferManager` 的 offset 步长 / seq 推导一律使用 `backend->chunkSize()`，不再硬编码




### 8.6 实施状态（2026-09-17 更新）
| 项 | 状态 |
|---|---|
| §8.1 `purge()`=真实 DELETE；回收站 unsupported | ✅ 已实现（`HttpBackend`） |
| §8.2 `setUploadOverwrite(bool)` | ✅ 已实现（`Backend.h` + `HttpBackend`） |
| §8.3 TOFU 由界面注入回调 | ✅ 已实现：`HttpBackend::setTrustPrompt`；**唯一注入者 = `AppController::installTrustPrompt()`**（其 `requestTrust()` 用 `receivers()==0` 守卫 + 60s 超时实现 fail-closed）。⚠️ 早期文档曾写"桥接在 `app/Application::promptTrustSync()`"，**该代码已删除**，勿再按旧描述重复注入 |
| §8.4 覆盖必须原子（禁先删再传） | ✅ 已返工（`TransferManager` 已删除 `purge` 调用） |
| §8.5 `chunkSize()` 单一来源 | ✅ 已实现；`Settings::chunkSizeMB()` 在 v1 停用（后续要用旋钮必须走本接口） |
| §4.1 角色语义 | ✅ 已实现（`data/` 6 模型，lead 复核 0 error/0 warning） |
| 构建 | ✅ `qmake && mingw32-make` 全量通过，产物 `bin/CloudVault.exe`（不含 QtWidgets） |


### 8.7 传输队列历史分组 + 自动刷新（加法式）

对应需求：「自动刷新 任务队列；队列任务完成后 加入到 已下载/已上传（默认隐藏）；文件列表自动刷新」。
**全部为加法式改动，未改任何冻结签名 / 冻结角色名。**

**（1）终端态任务移出活动队列、进入历史**
- 当任务进入**终端态**（`Completed` / `Failed` / `Canceled`）时，`TransferModel::updateState()`
  用 `beginRemoveRows/endRemoveRows` 把该行从活动队列 `m_tasks` **移出**，并把**更新后的完整
  `TransferTask`**（含新 `state` 与 `message`）**头插**到 `m_history`（最新在前）。
- 历史上限 **200 条**，超出丢弃**尾部**（最旧）。
- 不变量：**同一任务只存在于 `m_tasks` 与 `m_history` 之一**。
- 该路径上发 `countChanged()`（活动队列变短）与 `historyChanged()`；**不再**对已移出的行发 `dataChanged`。
- `updateProgress()` / `updateState()` / `removeTask()` 对**已不在 `m_tasks` 中**的 id **静默返回**
  （`TransferController` 在任务结束后仍可能收到迟到信号；不得崩溃、不得误插入新行）。
- 既有语义不变：`setTasks()` / `clear()` 仍只作用于活动表；清历史是新增的 `clearHistory()`。

**（2）历史读取接口（键名与角色同义）**
- `TransferModel` 既有角色名（`taskId fileName kind done total progress state message` +
  `progressRatio stateText`）**一个都没改、没删**；本任务**未新增角色**。
- 历史数据经 `Q_INVOKABLE` 提供，**键名与角色名同义同类型**：
  `uploadHistoryAt(i)` / `downloadHistoryAt(i)`（`0` = 最近一次；越界返回空 map）、
  `historyCount` / `uploadHistoryCount` / `downloadHistoryCount`（均 `NOTIFY historyChanged`）、
  `clearHistory()`。
- 历史 map **另加两个键**：`sizeText`（已完成字节的人类可读串，复用 `Util::humanSize`，保证全项目文案一致）、
  `displayPath`（上传 → `remotePath` 远端目标目录；下载 → `localPath` 本地保存路径）。
- `state` / `stateText` 复用本文件既有 `stateToken()` / `stateLabel()`，与活动队列同一套 token。

**（3）自动刷新配置（加法式）**
- `Settings`：`autoRefreshEnabled()`（默认 `true`）/ `autoRefreshIntervalSec()`（默认 `30`，
  合法范围 **5..3600**，越界钳制到边界；读取时也钳制防手改 INI）。
- `AppController` 新增 QML 属性 `autoRefresh` / `autoRefreshInterval`（READ/WRITE + 各一 NOTIFY），
  getter/setter 读写 `Settings` 并**立即落盘**（`Settings::sync()`）。

**（4）自动刷新采用「失败即暂停」策略（关键约束）**
- 因为 **§9-L1**：控制器路径的网络调用**在主线程同步执行**（`HttpBackend` 超时 `kTimeoutMs = 60000`），
  若在服务器不可达时**周期性**触发 `Files.refresh()` / `Stats.refresh()`，将导致 UI **反复冻结 60s**。
- 因此轮询**不得**无条件进行：**一次刷新失败即暂停轮询**（停止定时器），须**人工刷新成功后才恢复**自动轮询。
- QML 侧（由界面实现方负责）：关闭 `autoRefresh` 时不启动定时器；定时器间隔取 `App.autoRefreshInterval`（秒）。

**（5）L1 残留风险与后续建议**
- 本改动**不消除** §9-L1：即便有「失败即暂停」，首次自动刷新仍可能在服务器不可达时冻结 UI 最长 60s。
- **建议**：后续把控制器网络调用改异步（或移入线程池 + 信号回主线程），再放开无条件自动轮询。

**（6）自检钩子**
- `main.cpp` 新增环境变量 **`CV_SELFTEST_SEED_HISTORY=1`**：启动时向 `TransferModel` 种
  2 条假历史（1 条 Upload+Completed、1 条 Download+Failed），用于无头截图验证「已上传/已下载」分组渲染；
  未设置该环境变量时**零副作用**。种法：`addTask()` → `updateState(终端态)`，走真实「移出→入历史」路径。
- `main.cpp` 新增环境变量 **`CV_SCREENSHOT_DELAY_MS=<毫秒>`**（§8.8 引入）：覆盖 `CV_SCREENSHOT`
  抓图延时（默认 1800ms，保持原行为）。因启动期 toast 仅存活 ~4.5s，固定 1800ms 可能错过；需要抓取
  启动期瞬时 UI 时传更小值（如 `0`）。未设置时行为与改动前完全一致。

### 8.8 `FileController::refreshFinished(bool)` —— 驱动「自动刷新失败即暂停 / 成功即恢复」（加法式）

**背景（QA 证实的 P0）**：§8.7 的「失败即暂停」原实现挂在 `FileController::statusMessage(message, ok)`
上，但 `statusMessage` 全项目仅 4 处 `emit`、**全部 `ok=true`**，`refresh()` 的失败路径只发
`errorOccurred` + `logMessage`、成功路径只发 `logMessage` —— 于是**两个方向都是死路径**：
失败永不暂停、成功永不恢复。后果：服务器不可达时，30s 定时器每轮触发同步阻塞 `refresh()`
（`HttpBackend` 超时 60s）→ UI 周期性冻结（QA 实测单次 WALL≈89s）。

**裁决**：新增专用信号 **`FileController::refreshFinished(bool ok)`**，`refresh()` 的**每一个出口恰好 `emit` 一次**：
- 退出① `!m_backend`（早退）→ `emit refreshFinished(false)`
- 退出② `!r.ok`（列出失败）→ `emit refreshFinished(false)`
- 退出③ 成功收尾 → `emit refreshFinished(true)`

**为何不直接给 `refresh()` 补 `statusMessage`**：`qml/Main.qml` 亦 `target: Files` 并
`onStatusMessage → window.showToast(message, ok)`，而 `showToast` 对**任何非空 message 都弹**（不分 ok）。
若在成功路径补 `statusMessage`，会让**每 30 秒自动刷新都弹一次 toast**，比原缺陷更糟。
故持久化提示只能走「专用信号 + QML 侧自行节流」：`FilesPage.qml` 的 `onRefreshFinished(ok)`
仅在 `App.autoRefresh && !autoRefreshPaused` 时暂停并**一次性**提示，成功则恢复。

**界面契约（QML 义务）**：
- `Files.refreshFinished(true)` → 清除 `autoRefreshPaused`（恢复轮询）
- `Files.refreshFinished(false)` → 若自动刷新开启则暂停轮询并提示一次（已暂停则不再重复提示）
- 手动【刷新】按钮：点击时**先清 `autoRefreshPaused` 再 `Files.refresh()`**，使手动重试重新武装自动刷新；
  若仍失败，随后的 `refreshFinished(false)` 立即再次暂停。

**P2 补齐：同 id 不变量** —— `TransferModel::upsertTask()` 在插入活动表前，若该 id 已在 `m_history` 中，
先移除历史项并 `emit historyChanged()`，使代码与 `TransferModel.h` 已文档化的不变量
「同一任务只存在于 `m_tasks` 与 `m_history` 之一」**二者一致**（此前「终端态后再 `addTask` 同 id」会破坏它）。

---

## 9. 已知限制（v1，经独立 QA 验证后确认，非缺陷但需知悉）

| # | 限制 | 影响 | 后续建议 |
|---|---|---|---|
| L1 | **控制器路径的网络调用在主线程同步执行**（`FileController::refresh/rename/remove/createFolder`、`AppController::healthCheck/refreshStorage`、`StatsController::refresh` 直接调用阻塞式 `Backend`，`HttpBackend` 超时 60s） | 服务器不可达时 **UI 最长冻结 60s** | 后续改为异步（或把请求移入线程池 + 信号回主线程）；过渡期可先把超时调到 10–15s |
| L2 | 传输队列的 401 文案已映射为可操作提示，但 `TransferManager` 内部仍原样透出 `HTTP xxx` | 影响文案一致性（已由 `TransferController` 兜住） | 后续在 net 层统一错误语义 |
| L3 | `FileListModel.status`（秒传标记）恒为 `-`（服务端列表接口不返回 instant 字段），QML 的秒传徽标为**死代码** | 列显示无意义 | 要么由 manifest 回填，要么从界面移除该列 |
| L4 | 下载目录：`TransferManager` 用 `AppPaths::downloadDir()`，`TransferController::openLocalFolder()` 用 `Settings::downloadDir()` | 二者可能不一致 | 统一为 `Settings::downloadDir()` 并暴露给 QML |
| L5 | 本地下载收尾是"先删后改名"（非原子） | 改名失败会丢本地同名旧文件（可重下，低危） | 改为 `QFile::rename` 覆盖语义或先备份 |
| L6 | 服务端 `/api/v1/files` 单次上限 500 条（无分页） | 文件数 >500 时列表不全 | 服务端加分页（独立任务） |
| L7 | 深色模式未启用（`qtquickcontrols2.conf` 固定 Light） | 无法跟随系统深色 | 切换 conf + `Theme.darkMode` 即可（令牌已就位） |
| L8 | 冗余 API（QML 未使用）：`Files.emptyText/supported`、`Stats.freeBytes/totalBytes/supported`、`TransferController.supported`、`Auth/Sync` 全部属性、`Core` 上下文属性 | 维护噪声 | 后续收敛 |

# 云匣 CloudVault 正式客户端 · 接口契约（冻结）

> 本文件是 `src/` 各层并行实现的**唯一接口依据**。任何一方需要变更接口，必须先改本文件并通知全体实现方。
> 目标形态：**QML 界面 + 分层 C++**，能力母体为已验证的 `client/`（契约正确），服务端不支持的能力「接口保留、UI 不暴露」。

---

## 0. 分层与依赖规则（禁止跨层反向依赖）

```
qml/  ──调用──▶  controllers/  ──调用──▶  transfer/ · net/ · sync/ · core/
                     │                        ▲
                     └──使用──▶  data/（QML 数据模型）
app/  负责装配：创建 net/transfer/sync + controllers + models，注册到 QML 上下文
```
- QML **只**调用 `controllers/` 与 `data/` 暴露的属性/方法/角色；不直接触碰 `net/`、`transfer/`、`core/`
- `net/`、`transfer/`、`sync/`、`core/` 不 include `controllers/`、`data/`、`qml/`

---

## 1. `net/` —— 服务端真实契约（由 net 实现方负责）

- 抽象接口 `Backend.h` **保持完整**（产品蓝图不删）；服务端不具备的能力返回 **`Unsupported`** 结果，不发请求
- 契约细节以 `server/README.md` + `server/src/app/main.cpp` 的 `server.route(...)` 为准；调用方式对齐 `client/src/MainWindow.cpp`（唯一已验证实现）
- 统一出口负责：URL 拼装 + 百分号编码、`Authorization: Bearer <token>`、HTTP 状态码与 JSON `error` 映射、**TOFU 证书固定**（首次确认→记住 SHA-256→不一致拒绝；弹窗必须 `exec()`）
- 可识别结果：**同名冲突（409）**、缺块（409 missing）、哈希不符（422）、空间不足（507）、未授权（401）、不支持（本地枚举）

**Unsupported 清单**：登录/用户体系、回收站、版本、标签、搜索、parentId 目录体系（服务端用 `dir` 路径字符串）

> **分享链接已实现（2026-09-18 更新，从 Unsupported 清单移除）**：服务端新增
> `POST /api/v1/shares`、`GET /api/v1/shares`、`DELETE /api/v1/shares/:id`；
> **公开端点 `GET /s/:token`（+ `/meta`）免鉴权**，由服务端提供，客户端**不调用**，仅展示完整 URL。见 §8.14。

---

## 2. `transfer/TransferManager` —— 传输队列（冻结 API）

职责：分块上传（含秒传、断点续传、并发 3 块）、Range 下载（4 路窗口、按序落盘、断点）、队列与进度。逻辑移植自 `client/src/MainWindow.cpp` 的已验证实现。

```cpp
class TransferManager : public QObject {
    Q_OBJECT
    Q_PROPERTY(int     activeCount    READ activeCount    NOTIFY changed)
    Q_PROPERTY(QString statusText     READ statusText     NOTIFY changed)  // "… 上传中 3/8"
    Q_PROPERTY(bool    busy           READ busy           NOTIFY changed)

public:
    // 上传：本地文件绝对路径列表 → 远端目录（dir 为相对路径，'' = 根目录）
    Q_INVOKABLE void enqueueUpload(const QStringList &localPaths, const QString &dir);
    // 下载：fileId 列表 → 本地保存目录
    Q_INVOKABLE void enqueueDownload(const QStringList &fileIds, const QString &destDir);
    Q_INVOKABLE void cancelCurrent();

signals:
    void changed();
    void taskProgress(const QString &taskId, qint64 done, qint64 total);   // 进度
    void taskFinished(const QString &taskId, bool ok, const QString &message);
    void conflictDetected(const QString &taskId, const QString &dir, const QString &name);
    // ↑ 同名冲突：由 TransferController 弹"是否覆盖"，再回调解续
    void logMessage(const QString &level, const QString &text);

public slots:
    void resolveConflict(const QString &taskId, bool overwrite);   // 覆盖=true / 跳过=false
};
```

**关键实现约束**（都是踩过的坑，务必保留）
1. 分块上传：`init` → 3 块并发 `PUT chunk`（带 `X-Chunk-SHA256`）→ `complete`；秒传（`done:true`）直接完成
2. **本地 manifest** 记录 `size/mtime/full_hash` 三者一致才续传；`complete` 成功后删除
3. Range 下载：**4 路并发窗口**，乱序到达先缓存、**按序落盘**（写盘串行化，防写坏 `.part`）；`.part` 追加语义保证断点有效
4. 大文件哈希与读块**必须离开 UI 线程**（用 `QThreadPool`/`QtConcurrent`）；工作线程只碰值拷贝，结果经 `QMetaObject::invokeMethod(..., QueuedConnection)` 回主线程
5. 内存与文件大小解耦：绝不整文件读入内存（整文件只走 ≤ 8 MiB 的小文件路径）

---

## 3. `controllers/` —— QML 的唯一 C++ 入口（冻结 API）

| 控制器 | 属性 | 方法（Q_INVOKABLE / slots） | 备注 |
|---|---|---|---|
| `AppController` | `serverUrl` `accessToken` `statusText` `trustSelfSigned` `unsupportedNotice` | `healthCheck()` `refreshStorage()` `saveSettings()` | 已有骨架，补齐真实调用 |
| `FileController` | `currentDir` `loading` `emptyText` | `refresh()` `enterDir(dir)` `createFile(name)` `rename(id,name)` `remove(id)` `createFolder(path)` `uploadHere()` `downloadSelected()` | 列表数据走 `FileListModel` |
| `TransferController` | `activeCount` `statusText` `busy` | `upload(paths,dir)` `download(ids,dest)` `cancel()` `resolveConflict(id,overwrite)` `openLocalFolder()` | 包装 `TransferManager` |
| `StatsController` | `freeBytes` `totalBytes` `freeText` | `refresh()` | 对接 `GET /api/v1/storage` |
| `AuthController` | `supported=false` `userName=""` | — | **服务端不支持**，UI 不暴露入口 |
| `SyncController` | `supported=false` | — | 同上（`sync/` 地基保留，后续启用） |
| `ShareController` | `supported=false` | — | 同上 |

**未支持功能的呈现规则**：`supported=false` 的控制器注册 `unsupportedNotice`（中文说明"服务端暂不支持此功能"）。QML 中对应页面保留但**入口置灰 + tooltip 说明**，不出现可点击后失败的假功能。

---

## 4. `data/` —— QML 数据模型（冻结角色名）

| 模型 | 角色（roleNames） |
|---|---|
| `FileListModel` | `name` `size` `time` `status` `id` `dir`（`id/dir` 供右键菜单使用，界面可不显示） |
| `TransferModel` | `taskId` `fileName` `kind`(upload/download) `done` `total` `progress` `state` `message` |
| 其余（`SyncPairModel`/`ShareLinkModel`/`VersionModel`/`TagModel`） | 保留骨架、`rowCount()==0`，不暴露入口 |

### 4.1 角色语义（**冻结，QML 必须按此绑定**）

`FileListModel`
| 角色 | 类型 | 语义 |
|---|---|---|
| `name` | string | 文件/目录名 |
| `size` | **string（已格式化）** | 展示用，如 `1.23 MB`（对齐母体 `formatSize`） |
| `sizeBytes` | qint64 | **原始字节**（排序/逻辑判断用它） |
| `time` | **string（已格式化）** | 展示用，如 `2026-09-17 08:22`（对齐母体 `formatTime`） |
| `mtime` | qint64 | 原始时间戳 |
| `status` | string | 秒传标记：`是` / `否` / `-`（对齐母体 instantText） |
| `id` | string | 文件 id；目录可用合成 id |
| `dir` | string | 所属目录（相对路径，`""` = 根目录） |
| `hash` | string | 内容哈希（默认不显示） |

> ⚠️ **QML 注意**：`size` 与 `time` 是**字符串**，直接绑定即可显示；需要参与计算/比较时用 `sizeBytes` / `mtime`。

`TransferModel`
| 角色 | 类型 | 语义 |
|---|---|---|
| `taskId` | string | 任务标识 |
| `fileName` | string | 文件名 |
| `kind` | string | `upload` / `download` |
| `done` / `total` | qint64 | 已完成/总字节 |
| `progress` | int | **0..100**（百分比整数） |
| `progressRatio` | double | **0.0..1.0**（`ProgressBar.value` 推荐用它，因为该控件默认区间是 0–1） |
| `state` | string | 逻辑用英文 token：`queued`/`hashing`/`running`/`paused`/`completed`/`failed`/`canceled` |
| `stateText` | string | 显示用中文（如"传输中"） |
| `message` | string | 附加信息/错误文本 |

**排序在模型内完成**（`FileListModel::sortBy(int key, bool asc)`，key 0=名称 1=大小 2=时间；行为已对齐母体 `applySort`），**QML 不做排序**。
**模型只在主线程读写**（网络/IO 结果须经信号回主线程后再改模型），全程无锁。


---

## 5. `qml/` —— 界面文件布局（冻结）

```
qml/Main.qml                     主窗口：head 工具栏 + 内容区 + 状态条 + 传输抽屉（默认隐藏）
qml/theme/Theme.qml              Singleton 设计令牌（色彩/字号/间距/圆角），对应 docs/UI优化方案.md §2.1
qml/controls/                    复用控件：PrimaryButton / SecondaryButton / GhostButton / DangerButton /
                                 StatusBadge / EmptyState / SectionCard / SearchField / HistoryGroup
qml/panels/PreviewPanel.qml      右侧常驻预览模块（仅 wide + 文件页；按 previewState 分派）
qml/panels/TransferDrawer.qml    传输抽屉内容（进行中 + 已上传 + 已下载）
qml/pages/FilesPage.qml          文件列表（含空态、加载态、右键菜单，右键「分享…」入口）
qml/pages/TransfersPage.qml      传输队列（进度、取消、冲突处理）
qml/pages/SharesPage.qml         分享链接管理（supported=false 回落占位页）
qml/pages/SettingsPage.qml       连接设置（地址/令牌/证书指纹/自动刷新/本机磁盘/下载目录）
qml/pages/{TagsPage,TrashPage,VersionsPage}.qml   置灰占位页（"服务端暂不支持"）
qml/dialogs/                     新建文件 / 重命名 / 删除确认 / 覆盖询问 / 证书指纹确认 / 关于 / 分享
```
**响应式三档（`Theme.qml` 中集中定义断点）**：`≥1280` 三栏（列表+预览+传输）· `768–1280` 预览变抽屉 · `<768` 单栏 + 底部标签页。

---

## 6. 验证方式（所有实现方统一）

```bash
cd /d/Qtproject/File/build_official
export PATH="/d/Qt/6.9.3/mingw_64/bin:/d/Qt/Tools/mingw1310_64/bin:$PATH"
mingw32-make -f Makefile.Release ../build/obj/<你的文件>.o     # 逐文件
# 全量（等所有缺口补齐后）：
/d/Qt/6.9.3/mingw_64/bin/qmake.exe ../File.pro && mingw32-make -j4
```
- 要求：**RC=0、无 error**；沙箱偶发拦截编译器（RC≠0 且 stderr 全空）→ 重试
- 服务端是 POSIX，本机跑不起来 → 端到端联调需用户在 Debian 上执行

---

## 7. 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-09-17 | 首次冻结 | 三路并行实现前统一接口 |
| 2026-09-17 | 追加「8. 决策记录（lead 裁决）」 | net 实现方提出 2 个语义冲突点 |
| 2026-09-18 | 追加 §8.7「传输队列历史分组 + 自动刷新」（加法式） | 用户需求：队列任务完成后归入「已下载/已上传」（默认隐藏）、文件列表自动刷新 |
| 2026-09-18 | 追加 §8.8：`FileController::refreshFinished(bool)` 信号（加法式）；`TransferModel` 补齐「同 id 不同时存在于活动表与历史」不变量 | 修复 QA 证实的 P0：「自动刷新失败即暂停」为死路径（statusMessage 从不在失败/成功路径发出）；P2：同 id 可同时存在于两处 |
| 2026-09-18 | 追加 §8.10：`Backend` 异步变体 `listFolderAsync` / `usageAsync`（加法式、默认退化同步）；`FileController::refresh()` / `StatsController::refresh()` 改为**非阻塞** | 修复启动 89s 冻结：`HttpBackend` 用嵌套 `QEventLoop` 同步阻塞 GUI 主线程（超时 60s × 多次请求） |
| 2026-09-18 | 追加 §8.13（C++）：控制器 `logMessage` 接线到统一日志 sink；`FileController::refreshIfChanged()`（内容比对轮询） | 修复「控制器日志信号全是死信号，日志被吞」；自动刷新需轮询入口 |
| 2026-09-18 | 追加 §8.14（C++）：轮询内容指纹纳入 `hash` | 服务端覆盖同名文件不改 `created_at`，仅凭 id/name/size/mtime 检不到「同 size 覆盖」 |
| 2026-09-18 | 追加 §8.15（C++）：用量自动刷新 + `StatsController::localDisks` + `AppController::downloadDir`/`TransferManager::defaultDownloadDir` + 预览 `getRangeAsync`/`FileController` 预览 API | 用户需求：上传后剩余空间更新、下载目录设置生效、文件预览（异步不阻塞）；配套 §8.11 QML 侧 |
| 2026-09-18 | 追加 §8.16（C++）：分享链接实装（`Backend` 异步变体 + `HttpBackend` 真请求 + `ShareController`） | 服务端已实现 `POST/GET /api/v1/shares`、`DELETE /api/v1/shares/:id`；从 §1 Unsupported 清单移除分享；配套 §8.12 QML 侧 |
| 2026-09-18 | 追加 §8.11「右侧预览模块 + 传输队列改默认隐藏抽屉 + 本机磁盘 / 可编辑下载目录（QML 侧，加法式）」 | 用户需求：右侧区改预览模块、传输队列默认隐藏点击展开、上传后剩余空间更新 + 各磁盘容量、下载路径可编辑 |
| 2026-09-18 | 追加 §8.12「分享链接页 + 新建分享对话框 + 文件页右键入口（QML 侧，加法式）」 | 用户需求：用真实分享管理页替换占位页 |
| 2026-09-18 | §8.12 增补「分享链接可用性完善」：打开链接（`Qt.openUrlExternally`）+ 提取码一次性 / 链接形态说明 | 用户需求：完善分享链接（可下载 / 提取码 / 有效期） |
| 2026-09-18 | 追加 §8.17「证书指纹不匹配对话框 + 恢复路径（QML 侧，加法式）」：`qml/dialogs/CertMismatchDialog.qml` + `Main.qml` 接线（`App.certPinMismatch` 去重、`App.clearPinnedFingerprint()` 显式清除） | 用户需求：证书变更时给出明确提示与恢复路径；保持 fail-closed、绝不自动信任 |

---

## 8. 决策记录（lead 裁决，全体实现方遵守）

### 8.1 删除语义：`purge()` = 真实永久删除；回收站一族 = unsupported
- **裁决**：`purge(id)` **映射到真实接口 `DELETE /api/v1/files/{id}`**（服务端该接口即"删除并回收空间"，返回 `freed_bytes`）；`moveToTrash`/`restore`/`listTrash`/`emptyTrash` 一律 `unsupported`（服务端无回收站）。
- **理由**：若严格按字面把 `purge` 也列为 unsupported，正式客户端将**没有任何删除路径**，与产品能力不符。服务端语义是"硬删除"，故用 `purge` 承载它最贴切。
- **UI 影响**：文件右键"删除"= 真实删除；文案必须明确「删除后不可恢复」（因为服务端没有回收站）。

### 8.2 分块上传的"同名覆盖"：新增**加法式**接口（不改既有签名）
- **问题**：`Backend.h::beginUpload(parentId,name,size,sha256)` 不携带 `overwrite`，导致分块上传只能把 409 当错误、无法覆盖。
- **裁决**：在 `Backend.h` **新增一个带默认实现的虚函数**（对既有实现零破坏）：
  ```cpp
  // 上传同名文件时是否覆盖（仅对"整文件上传"与"分块 init"生效）。默认 false。
  virtual void setUploadOverwrite(bool enabled) { (void)enabled; }
  ```
  - `HttpBackend` 存储该标志：① 整文件上传 → 头 `X-CV-Overwrite: 1`；② 分块 `init` body → `"overwrite": true`
  - `MockBackend` 继承默认实现（no-op），**不需要改**
- **调用方约定（`TransferManager`）**：收到 `conflictDetected` → 上层 `resolveConflict(taskId, true)` 时，先 `backend->setUploadOverwrite(true)`，其余情况保持 `false`；一次覆盖只对随后的那一次上传生效，用后立即复位。

### 8.3 TOFU 确认框：net 层不弹窗，由界面注入回调（QML）
- `src/net/HttpBackend` **不依赖 QtWidgets**（正式客户端链接里没有 Qt6Widgets）。它暴露 `setTrustPrompt(回调)` 由界面层注入确认逻辑；**未注入回调时一律 fail-closed（拒绝连接）**，绝不静默信任。
- **唯一注入者 = `AppController`**：它在构造时调用 `installTrustPrompt()` 注入回调，并通过 `trustPromptRequested` / `answerTrustPrompt(bool)` 与 QML 交互（内部有 `receivers()==0` 守卫，无人接线即拒绝）。
- ⚠️ **`HttpBackend::setTrustPrompt()` 是「赋值」语义，不是追加** —— 全工程**只允许一个注入者**。装配层（`app/Application`）**不得**再次注入，否则后注入者会覆盖前者，导致 QML 只连到其中一条、另一条成为死路径（此缺陷已发生过一次：曾出现"弹窗永不出现 + fail-closed 拒绝连接"）。
- **QML 侧义务**：`CertPinDialog` 绑定 **`App.trustPromptRequested`**，回答时调 **`App.answerTrustPrompt(bool)`**——首次连接展示 `host:port` + 分组指纹(SHA-256) + subject/issuer/有效期；选"信任"→ 写入 `pinnedFingerprint/<host:port>` 并放行；选"不信任"→ 拒绝；**指纹不一致 → 警告且不提供"继续"**。不要再连 `Core.*`（`Application` 的同名 API 已删除）。
- 指纹持久化文件与 `client/` **不同**（正式客户端用 `AppPaths::settingsFile()`），两者不共享信任记录——这是预期行为，文档需说明。

### 8.4 覆盖同名文件**必须原子**：禁止"先删再传"
- **问题**：`TransferManager` 初版把"覆盖"实现为 `purge(file_id)` 删除旧文件 → 再重新 `init` 上传。这是**两步非原子操作**：若中途失败/断电，用户会**永久丢失原文件**（服务端无回收站，删了就没了）。
- **裁决**：覆盖一律走**服务端原子覆盖语义**：
  - 整文件上传：请求头 `X-CV-Overwrite: 1`
  - 分块上传：`init` body 带 `"overwrite": true`（会话在 `complete` 时替换同名记录内容，失败时旧文件保持不变）
  - 触发方式：`backend->setUploadOverwrite(true)` → 重新发起该次上传 → **发起后立即复位为 false**
- **禁止**：任何形式的"先删旧文件再传新文件"覆盖实现。

### 8.5 分块大小：**单一来源**，由后端暴露
- **问题**：`TransferManager` 用常量 5 MiB 计算 offset→seq，而 `HttpBackend` 从 `Settings::chunkSizeMB()` 取块大小（可被用户改动）。两者一旦不一致，`putChunk` 会被服务端判 400（seq 与 offset 不匹配）。
- **裁决**：在 `Backend.h` 新增（带默认实现，加法式）：
  ```cpp
  virtual qint64 chunkSize() const { return 5 * 1024 * 1024; }   // 分块上传的块大小（字节），唯一来源
  ```
  - `HttpBackend` 覆盖它，返回**它 init 时实际使用**的块大小；并且 **init 直接用该返回值**，不再从 `Settings::chunkSizeMB()` 读取（避免留下一个会让两端失配的隐藏旋钮）
  - `TransferManager` 的 offset 步长 / seq 推导一律使用 `backend->chunkSize()`，不再硬编码




### 8.6 实施状态（2026-09-17 更新）
| 项 | 状态 |
|---|---|
| §8.1 `purge()`=真实 DELETE；回收站 unsupported | ✅ 已实现（`HttpBackend`） |
| §8.2 `setUploadOverwrite(bool)` | ✅ 已实现（`Backend.h` + `HttpBackend`） |
| §8.3 TOFU 由界面注入回调 | ✅ 已实现：`HttpBackend::setTrustPrompt`；**唯一注入者 = `AppController::installTrustPrompt()`**（其 `requestTrust()` 用 `receivers()==0` 守卫 + 60s 超时实现 fail-closed）。⚠️ 早期文档曾写"桥接在 `app/Application::promptTrustSync()`"，**该代码已删除**，勿再按旧描述重复注入 |
| §8.4 覆盖必须原子（禁先删再传） | ✅ 已返工（`TransferManager` 已删除 `purge` 调用） |
| §8.5 `chunkSize()` 单一来源 | ✅ 已实现；`Settings::chunkSizeMB()` 在 v1 停用（后续要用旋钮必须走本接口） |
| §4.1 角色语义 | ✅ 已实现（`data/` 6 模型，lead 复核 0 error/0 warning） |
| 构建 | ✅ `qmake && mingw32-make` 全量通过，产物 `bin/CloudVault.exe`（不含 QtWidgets） |


### 8.7 传输队列历史分组 + 自动刷新（加法式）

对应需求：「自动刷新 任务队列；队列任务完成后 加入到 已下载/已上传（默认隐藏）；文件列表自动刷新」。
**全部为加法式改动，未改任何冻结签名 / 冻结角色名。**

**（1）终端态任务移出活动队列、进入历史**
- 当任务进入**终端态**（`Completed` / `Failed` / `Canceled`）时，`TransferModel::updateState()`
  用 `beginRemoveRows/endRemoveRows` 把该行从活动队列 `m_tasks` **移出**，并把**更新后的完整
  `TransferTask`**（含新 `state` 与 `message`）**头插**到 `m_history`（最新在前）。
- 历史上限 **200 条**，超出丢弃**尾部**（最旧）。
- 不变量：**同一任务只存在于 `m_tasks` 与 `m_history` 之一**。
- 该路径上发 `countChanged()`（活动队列变短）与 `historyChanged()`；**不再**对已移出的行发 `dataChanged`。
- `updateProgress()` / `updateState()` / `removeTask()` 对**已不在 `m_tasks` 中**的 id **静默返回**
  （`TransferController` 在任务结束后仍可能收到迟到信号；不得崩溃、不得误插入新行）。
- 既有语义不变：`setTasks()` / `clear()` 仍只作用于活动表；清历史是新增的 `clearHistory()`。

**（2）历史读取接口（键名与角色同义）**
- `TransferModel` 既有角色名（`taskId fileName kind done total progress state message` +
  `progressRatio stateText`）**一个都没改、没删**；本任务**未新增角色**。
- 历史数据经 `Q_INVOKABLE` 提供，**键名与角色名同义同类型**：
  `uploadHistoryAt(i)` / `downloadHistoryAt(i)`（`0` = 最近一次；越界返回空 map）、
  `historyCount` / `uploadHistoryCount` / `downloadHistoryCount`（均 `NOTIFY historyChanged`）、
  `clearHistory()`。
- 历史 map **另加两个键**：`sizeText`（已完成字节的人类可读串，复用 `Util::humanSize`，保证全项目文案一致）、
  `displayPath`（上传 → `remotePath` 远端目标目录；下载 → `localPath` 本地保存路径）。
- `state` / `stateText` 复用本文件既有 `stateToken()` / `stateLabel()`，与活动队列同一套 token。

**（3）自动刷新配置（加法式）**
- `Settings`：`autoRefreshEnabled()`（默认 `true`）/ `autoRefreshIntervalSec()`（默认 `30`，
  合法范围 **5..3600**，越界钳制到边界；读取时也钳制防手改 INI）。
- `AppController` 新增 QML 属性 `autoRefresh` / `autoRefreshInterval`（READ/WRITE + 各一 NOTIFY），
  getter/setter 读写 `Settings` 并**立即落盘**（`Settings::sync()`）。

**（4）自动刷新采用「失败即暂停」策略（关键约束）**
- 因为 **§9-L1**：控制器路径的网络调用**在主线程同步执行**（`HttpBackend` 超时 `kTimeoutMs = 60000`），
  若在服务器不可达时**周期性**触发 `Files.refresh()` / `Stats.refresh()`，将导致 UI **反复冻结 60s**。
- 因此轮询**不得**无条件进行：**一次刷新失败即暂停轮询**（停止定时器），须**人工刷新成功后才恢复**自动轮询。
- QML 侧（由界面实现方负责）：关闭 `autoRefresh` 时不启动定时器；定时器间隔取 `App.autoRefreshInterval`（秒）。

**（5）L1 残留风险与后续建议**
- 本改动**不消除** §9-L1：即便有「失败即暂停」，首次自动刷新仍可能在服务器不可达时冻结 UI 最长 60s。
- **建议**：后续把控制器网络调用改异步（或移入线程池 + 信号回主线程），再放开无条件自动轮询。

**（6）自检钩子**
- `main.cpp` 新增环境变量 **`CV_SELFTEST_SEED_HISTORY=1`**：启动时向 `TransferModel` 种
  2 条假历史（1 条 Upload+Completed、1 条 Download+Failed），用于无头截图验证「已上传/已下载」分组渲染；
  未设置该环境变量时**零副作用**。种法：`addTask()` → `updateState(终端态)`，走真实「移出→入历史」路径。
- `main.cpp` 新增环境变量 **`CV_SCREENSHOT_DELAY_MS=<毫秒>`**（§8.8 引入）：覆盖 `CV_SCREENSHOT`
  抓图延时（默认 1800ms，保持原行为）。因启动期 toast 仅存活 ~4.5s，固定 1800ms 可能错过；需要抓取
  启动期瞬时 UI 时传更小值（如 `0`）。未设置时行为与改动前完全一致。

### 8.8 `FileController::refreshFinished(bool)` —— 驱动「自动刷新失败即暂停 / 成功即恢复」（加法式）

**背景（QA 证实的 P0）**：§8.7 的「失败即暂停」原实现挂在 `FileController::statusMessage(message, ok)`
上，但 `statusMessage` 全项目仅 4 处 `emit`、**全部 `ok=true`**，`refresh()` 的失败路径只发
`errorOccurred` + `logMessage`、成功路径只发 `logMessage` —— 于是**两个方向都是死路径**：
失败永不暂停、成功永不恢复。后果：服务器不可达时，30s 定时器每轮触发同步阻塞 `refresh()`
（`HttpBackend` 超时 60s）→ UI 周期性冻结（QA 实测单次 WALL≈89s）。

**裁决**：新增专用信号 **`FileController::refreshFinished(bool ok)`**，`refresh()` 的**每一个出口恰好 `emit` 一次**：
- 退出① `!m_backend`（早退）→ `emit refreshFinished(false)`
- 退出② `!r.ok`（列出失败）→ `emit refreshFinished(false)`
- 退出③ 成功收尾 → `emit refreshFinished(true)`

**为何不直接给 `refresh()` 补 `statusMessage`**：`qml/Main.qml` 亦 `target: Files` 并
`onStatusMessage → window.showToast(message, ok)`，而 `showToast` 对**任何非空 message 都弹**（不分 ok）。
若在成功路径补 `statusMessage`，会让**每 30 秒自动刷新都弹一次 toast**，比原缺陷更糟。
故持久化提示只能走「专用信号 + QML 侧自行节流」：`FilesPage.qml` 的 `onRefreshFinished(ok)`
仅在 `App.autoRefresh && !autoRefreshPaused` 时暂停并**一次性**提示，成功则恢复。

**界面契约（QML 义务）**：
- `Files.refreshFinished(true)` → 清除 `autoRefreshPaused`（恢复轮询）
- `Files.refreshFinished(false)` → 若自动刷新开启则暂停轮询并提示一次（已暂停则不再重复提示）
- 手动刷新入口（**工具栏【刷新】按钮已移除，见 §8.9**）：右键菜单「刷新」或 **F5** → `FilesPage::refreshNow()`：
  **先清 `autoRefreshPaused` 再 `Files.refresh()`**，使手动重试重新武装自动刷新；
  若仍失败，随后的 `refreshFinished(false)` 立即再次暂停。

**P2 补齐：同 id 不变量** —— `TransferModel::upsertTask()` 在插入活动表前，若该 id 已在 `m_history` 中，
先移除历史项并 `emit historyChanged()`，使代码与 `TransferModel.h` 已文档化的不变量
「同一任务只存在于 `m_tasks` 与 `m_history` 之一」**二者一致**（此前「终端态后再 `addTask` 同 id」会破坏它）。

### 8.9 QML 界面调整：刷新入口改右键/F5、轮询改「内容比对」、历史行进度式大小（加法式）

对应需求：「删除两个刷新按钮 / 改成右键刷新 + 文件变化自动刷新 / 大小改成『5 MB / 2.27 GB』进度式」。
**纯界面改动（`qml/`），未改任何冻结签名 / 角色名 / C++ 接口。**

**（1）删除文件页工具栏两个刷新按钮**
- 移除 `FilesPage.qml` 工具栏的手动【刷新】按钮与三态「自动 ⟳ …」按钮；工具栏剩余
  （搜索框 · 已选计数 · 新建 · 上传 · 下载）无空位。
- `autoRefreshPaused` 属性与「失败即暂停」逻辑**保留**（仍用于避免对不可达服务器空转），仅不再有按钮显示它。

**（2）手动刷新入口：右键菜单「刷新」+ F5**
- 右键上下文菜单新增首项「⟳ 刷新」→ `page.refreshNow()`。
- F5 / Ctrl+R：**已在 `Main.qml` 全局注册**（未重复添加，避免同序列快捷键冲突），现改为调用
  `filesPage.refreshNow()`（同样清暂停后刷新），并保留其 `Stats.refresh()`。

**（3）低频轮询改走 `refreshIfChanged()`**
- `FilesPage.qml` 自动刷新 Timer 的 `onTriggered` 由 `Files.refresh()` 改为 `Files.refreshIfChanged()`：
  **内容指纹未变则不动界面、不发信号**（避免闪烁、不打断选中/滚动）。
- 四个运行条件（`App.autoRefresh && page.visible && !Files.loading && !page.autoRefreshPaused`）与
  `interval`（`max(5, App.autoRefreshInterval)` 秒）保持不变。
- 设置页「自动刷新」卡片保留（Switch + 间隔 15/30/60/120），语义改为**检查间隔**：
  「每隔设定间隔检查一次服务器；只有文件发生变化时才刷新列表」。

**（4）操作后立即刷新**
- `FilesPage.qml` 新增 `Connections { target: TransferModel; onHistoryChanged }`：本客户端传输任务
  进入历史（完成/失败/取消，即服务端文件已变化）→ **立即 `Files.refresh()`**，不等下一次轮询。
- 仅在该事件触发一次刷新（不与 `FileModel.onCountChanged` 重复）；`autoRefreshPaused` 时不刷新，
  避免对已知不可达的服务器再发起阻塞（60s）请求。

**（5）历史行大小：进度式「已传 / 总量」**
- `TransfersPage.qml` 历史行大小字段由 `sizeText` 改为 **`sizeText + " / " + totalText`**
  （失败/中断的下载此前只显示已传部分，看不出总量）。
- `totalText`（人类可读总量，如 `2.27 GB`）由 `TransferModel::historyMapFor()` 新增；`sizeText`
  语义不变（= 已传输量，如 `1.05 GB`）。**QML 不自写字节格式化**，一律用这两个键。
- 该大小标签**不设 `elide`**，保证「总量」那一半不被吃掉；文件名侧负责省略。

**（6）上下文刷新入口统一走 `refreshNow()`（修复「看不见的暂停」）**
- 空列表态 CTA「刷新」与错误态「重试」（`FilesPage.qml` 空态 / 错误态 `EmptyState` 内）的 `onClicked`
  由 `Files.refresh()` 改为 `page.refreshNow()`：失败后 `autoRefreshPaused=true` 时点它，会**先清暂停再刷新**，
  避免出现「列表已出来、但轮询永远停在暂停态，且三态按钮已删除 → 暂停对用户完全不可见」的隐性缺陷。
- `qml/Main.qml` 顶部溢出菜单「刷新文件列表」同理由 `Files.refresh()` 改为 `filesPage.refreshNow()`。
- **入口审计结论**：`qml/` 内**所有用户可触发的刷新入口**（右键菜单、F5 / Ctrl+R、空态、错误态、溢出菜单）
  已统一走到 `refreshNow()`；仅 `Main.qml` 启动期 `Component.onCompleted` 的首次 `Files.refresh()` 保留
  （非交互入口；启动时 `autoRefreshPaused` 恒为 false，不存在残留暂停）。

**（7）弹层（Menu）调色板：实为 offscreen 平台假象 —— 真机正常，不改代码**
- 现象：offscreen 抓图下右键菜单 / 溢出菜单弹层呈**深色底**，与浅色应用冲突。核查结论：**无头平台假象**，非真缺陷。
- 证据（同一台机、同一系统，仅切换 QPA 平台；菜单内部同矩形 `(315,275)-(475,555)` 采样）：
  - **真机**（默认 `windows` 平台，`CV_SCREENSHOT`；本机 Windows 系统实为**深色** `AppsUseLightTheme=0`）：
    `dark(<100)=0%`，`light(>200)=96%`，`(475,300)=#FFFFFF` → **浅色**，与应用一致。
  - **offscreen**：`dark=65%`，`light=34%`（`#343434 / #353535`）→ 深色。
- 说明：`src/app/main.cpp` 已 `setColorScheme(Light)` + 显式 `setPalette(...)`；**真机在系统为深色时弹层仍为浅色**，
  证明应用调色板对弹层（独立窗口）同样生效。offscreen 平台会忽略 `setColorScheme` 且默认调色板偏深 → 抓图失真。
- **Dialog**（6 个 `qml/dialogs/*.qml`）：真机与 offscreen **均为浅色**（真机 dialog 内部 `light=92%`），无需处理。
- 决策：**不修改 Menu / Dialog 代码**（避免真机外观回归）；仅在此记一笔。**offscreen 截图不可用于弹层调色板判定。**

### 8.10 后端调用异步化：修复启动 89s 冻结（加法式）

**缺陷（QA 实测）**：`serverUrl` 不可达时，offscreen 启动到抓图回调实测 **WALL≈89s**。根因是**同步阻塞 GUI 主线程**：
`HttpBackend::request()` 用 `QEventLoop loop; loop.exec();` 把 QNAM 异步请求包成同步返回（超时 `kTimeoutMs=60000`），
而 `qml/Main.qml` 在 `Component.onCompleted` 里同步调 `Files.refresh()` / `Stats.refresh()`（各含多次 HTTP、1 次 usage 含 3 次请求）。

**裁决**：新增**异步变体**（`Backend.h` 加法式、带默认实现，默认退化同步，本地引擎等既有实现无需改动）：

```cpp
virtual void listFolderAsync(const QString &parentId,
                             std::function<void(Result<QVector<FileItem>>)> done) { done(listFolder(parentId)); }
virtual void usageAsync(std::function<void(Result<UsageStats>)> done) { done(usage()); }
```

- **`HttpBackend`** 真正实现异步：新私有 `requestAsync(Method, path, body, headers, done)`，
  **不建嵌套 `QEventLoop`**，由 `reply::finished` / `QTimer`(超时，仍 `abort()` reply) / `sslErrors`(仍走 `applyCertPinning`) 驱动回调；
  `done` **恰好调用一次**。同步 `request()` 改为**用局部 `QEventLoop` 包住 `requestAsync()`**——传输与解析只有一份（单一来源）。
- **解析共用**：`buildFolderItems()` / `buildUsage()` 为静态成员，同步与异步**共用同一份**，避免两条路径漂移。
- **`FileController::refresh()` / `StatsController::refresh()` 非阻塞**：`setLoading(true)` 后调 `*Async` 立即返回；
  回调里做原有处理并**恰好发一次** `refreshFinished(bool)`（§8.8 语义不变）/ `statsChanged()`。
  各自加**序号守卫**（`m_refreshSeq` / `m_usageSeq`）：回调内 `if (seq != 最新序号) return;`，丢弃乱序回包（快速切目录/连点刷新时旧回包作废）。
- **线程**：回调都发生在主线程（QNAM 信号），不引入跨线程；`nam()` 仍为 per-thread（`QThreadStorage`）。

**语义变更**：`FileController::refresh()` / `StatsController::refresh()` **不再阻塞主线程**——调用后立即返回，结果异步回填。
QML 已按属性/信号绑定，无需改动；`kTimeoutMs` 语义不变（仍含大分块 PUT / Range 下载）。

**仍为同步阻塞的调用点（本次未改，供后续裁决）**：

| 调用点 | 后端方法 | 备注 |
|---|---|---|
| `FileController::createFolder()` | `ensureFolderPath()` | 建目录前置检查 |
| `FileController::rename()` | `rename()` | |
| `FileController::remove()` | `purge()` | |
| `FileController::createFile()` | `beginUpload()` + `finishUpload()` | 新建空文件 |
| `AppController::healthCheck()` | `health()` / `listFolder()` | 手动触发（非启动关键路径） |
| `AppController::refreshStorage()` | `usage()` | 手动触发 |
| `TransferManager` 各传输方法 | `statById()` 等 | 传输 worker 线程，不卡 GUI |

> ⚠️ 这些点仍会在服务器不可达时阻塞其**调用线程**（主线程）最长 60s；手册触发场景影响可接受，如需彻底消除请后续按同一模式异步化。

### 8.11 界面布局调整：右侧预览模块 + 传输队列改默认隐藏抽屉 + 设置页本机磁盘/下载目录（QML 侧，加法式）

对应需求：「1 修复上传后剩余空间未更新；同时增加获取本机各磁盘空间；2 下载路径可编辑；3 文件栏右侧改预览模块、传输队列默认隐藏点击展开（含已下载/已上传）」。

**纯界面改动（`qml/` + `resources/`），未改任何冻结签名 / 角色名。** 依赖的 C++ 新 API 见本节末「冻结 API」，QML 侧一律 `typeof` 守卫（未落地时优雅降级）。

**（1）右侧区：传输侧栏 → 预览模块 `qml/panels/PreviewPanel.qml`（新增）**
- `Main.qml` 右侧常驻区由「传输侧栏摘要」改为 `PreviewPanel`，可见条件 `window.wide && window.navIndex === window.pageFiles`（仅宽屏且文件页；其它页面收掉面板腾出宽度）。
- 面板按 `Files.previewState` 分派：`idle`（未选中 → 引导文案）/ `loading`（BusyIndicator）/ `text`（`ScrollView` + 只读等宽 `TextArea`，`WrapAnywhere`）/ `image`（`Image.PreserveAspectFit`，`sourceSize` 受面板约束）/ `unsupported` / `error`（危险色 `previewError`）。
- 任何状态都显示元信息区（`previewMeta`：`name/isDir/sizeText/timeText/path/kind/hash`）。
- **typeof 守卫**：`Files.previewState === undefined` → 面板退化为 idle，不抛错（QML 对 C++ 符号是运行时解析）。

**（2）传输队列：默认隐藏 + 点击展开抽屉（`qml/panels/TransferDrawer.qml` 新增）**
- 工具栏「传输队列」按钮（宽/紧模式**共用同一个** Drawer）切换唯一 `Drawer`（`edge: RightEdge`）；带进行中数量徽标（`Transfers.activeCount`）。
- **默认隐藏**：`Drawer.opened` 只读（写它会导致组件加载失败），默认即关闭态；显式 `Component.onCompleted: close()` 把意图钉死。
- 抽屉内容 = 进行中队列（`TransferModel`）+ 「已上传」+「已下载」两栏（`HistoryGroup`，`alwaysShowHeader: true`）+「取消当前」。
- 抽屉与 `Main` 解耦：`closeRequested()` / `openFullPageRequested()` 两个信号；后者关闭抽屉并跳转「传输」整页。
- 导航「传输」整页 **保留不变**。

**（3）`HistoryGroup` 抽为共用控件 `qml/controls/HistoryGroup.qml`（新增）**
- 从原 `TransfersPage.qml` 内联 `component HistoryGroup` 抽出，供「传输整页」与「传输抽屉」两处复用（避免复制两份配色/交互）。
- 新增属性 `alwaysShowHeader`（抽屉内为空历史也保留栏头，让用户看到两栏存在）；`state→tone` 映射改走 `Theme.toneForState()`（`Theme` 新增，三处共用）。
- `TransfersPage.qml` 改为 `import "../controls"` + 使用 `HistoryGroup`；删去内联组件与本地 `stateTone()`。

**（4）预览状态同步（`FilesPage.qml`）**
- `syncPreview()`：仅在本页可见且**恰好选中 1 项**时 `Files.requestPreview(id)`，否则 `Files.clearPreview()`；用 `previewedId` 去抖（目标未变不重复请求）。
- 触发点：`onSelectedIdsChanged` / `onVisibleChanged`（离开文件页清空）；`FileModel.onCountChanged` 后补一次校验（删除/改名致目标消失 → 清空）。目录也请求预览（由后端决定 `unsupported`）。

**（5）设置页本机磁盘 + 可编辑下载目录（`SettingsPage.qml`）**
- 「本机磁盘空间」卡片：`Repeater` 遍历 `Stats.localDisks`，每项显示 `label` + `剩余 freeText / 共 totalText` + 用量条（`usedRatio`）+ `isDefault` 的「默认」徽标；「重新扫描」→ `Stats.refreshLocalDisks()`；`localDisks` 为空 → 「暂不可用」提示（未落地降级）。
- 「下载目录」卡片改为**可编辑**：等宽可选中路径 `TextField` + 【浏览…】（`QtQuick.Dialogs.FolderDialog` → `urlToPath` → `App.downloadDir`）+ 校验（空 / 非绝对路径 → 红字）+ 「● 未保存 / ✓ 已保存」提示（`downloadDirDirty`）。
- 持久化交既有【保存设置】（`App.saveSettings()`），保存成功处调用 `markDownloadDirSaved()`；`Component.onCompleted` 里 `syncDownloadDir()`，`onVisibleChanged` 里 `refreshLocalDisks()`。
- **远程容量保持显眼**（未改状态条 `Stats.freeText`）；「上传 / 删除后自动刷新剩余空间」由 C++ 负责，QML **不重复加**轮询。

**（6）qrc 新增登记**
`resources/resources.qrc` 新增：`qml/panels/PreviewPanel.qml`、`qml/panels/TransferDrawer.qml`、`qml/controls/HistoryGroup.qml`。

**依赖的冻结 C++ API（另一工位落地；QML 侧 typeof 守卫）**
```qml
Stats.localDisks           // QVariantList：{name,label,totalText,freeText,usedText,usedRatio(0..1),isDefault}
Stats.refreshLocalDisks()  // Q_INVOKABLE
App.downloadDir            // Q_PROPERTY 读写（持久化走既有 App.saveSettings()）
Files.requestPreview(fileId) / clearPreview()
Files.previewState / previewMeta / previewText / previewImageUrl / previewError
```

### 8.12 分享链接页 + 新建分享对话框 + 文件页右键入口（QML 侧，加法式）

对应需求：「分享链接界面（替换现在的占位页）」。

**纯界面改动（`qml/` + `resources/`），未改任何冻结签名。** 依赖的 `Shares` 冻结 API 尚未落地，QML 一律 `typeof` 守卫 + `supported===false` 回落占位。

**（1）`qml/pages/SharesPage.qml`：占位 → 真实页面**
- `Shares.supported === false`（或未就绪）时 **回落** 为既有 `UnsupportedPage`（占位保留，不删）。
- 列表 `ListView`（`model: Shares.count`，`delegate` 读 `Shares.at(index)`）；因 `at()` 是函数调用、QML 无法自动追踪依赖，用自增 `sharesVersion` 作**显式依赖**（`Connections.onSharesChanged → ++`）。
- 每项显示：图标（`isDir`）+ `fileName` + `sizeText` + 「已设提取码」徽标（`needCode`）+ `expireText` + `remainText` + 「已下载 N 次」（`maxDownloads>0` → `N/max`）。
- 链接一行 `url`（`Text.ElideMiddle` 中间省略）+ 【复制链接】→ `Shares.copyLink(url)`；【撤销】（DangerButton，**二次确认弹窗**）→ `Shares.revoke(id)`。
- `expired===true || usable===false` → 行 `opacity 0.62` 弱化 + 「已失效」徽标；仅保留复制 / 撤销。
- 空态 `EmptyState`「还没有分享链接」+ 引导；`busy` 时禁用「刷新 / 复制 / 撤销」；进页 `onVisibleChanged` → `Shares.refresh()`。
- 列表 **不显示提取码**（服务端只存哈希，列表 `code` 恒空），仅 `needCode` 徽标。

**（2）`qml/dialogs/ShareDialog.qml`（新增）**
- 输入：文件名（只读）+ 提取码（可空；4~6 位字母数字；【随机生成】4 位，剔除易混淆字符）+ 有效期下拉（1/7/30 天/永久 → `expireDays` 1/7/30/0，**默认 7 天**）+ 下载次数下拉（不限/1/5/10 → `maxDownloads` 0/1/5/10，**默认不限**）。
- 【创建】→ `Shares.create(fileId, code, expireDays, maxDownloads)`（`busy` 时禁用）；校验不满足就地红字。
- 两阶段：表单态 → 结果态。`Shares.onCreated(url, code)` → 结果态展示 **链接**（只读可选）+ **提取码**（只读大号；空 → 「无提取码」）+ 三个复制按钮（链接 / 提取码 / 链接和提取码拼接「链接：<url>\n提取码：<code>」）+ 醒目提示「提取码仅创建时显示一次，请及时保存」+ 【完成】关闭。
- `Shares.onErrorOccurred(message)` 就地红字（仅在弹出期间挂接连接）。

**（3）文件页右键「分享…」（`FilesPage.qml`）**
- 右键菜单「下载」旁新增 `🔗 分享…`；`enabled: page.selectedIds.length === 1`（0 个 / 多选禁用）；点击用**当前唯一选中项**的 `id`/`name` 打开 `ShareDialog`。
- 新增辅助 `nameForId(id)`（在过滤视图内反查名称）。

**（4）qrc 新增登记**
`resources/resources.qrc` 新增：`qml/dialogs/ShareDialog.qml`。

**（5）可用性完善：打开链接 + 说明文案（本轮增补，纯 QML，无 C++ 改动）**
- `SharesPage.qml` 每条分享项操作区在「复制链接」「撤销」之间新增「**打开**」（`GhostButton`，`glyph: 🌐`）→
  `Qt.openUrlExternally(shareRow.url)`（QtQml 内置，**不新增 C++**）；`enabled` 仅要求 `url` 非空且非 `busy`，
  **过期项仍允许打开**（浏览器会显示过期提示，正好可用于验证），按钮 `ToolTip` 对过期/正常给不同文案；
  `Qt.openUrlExternally` 返回 `false` → 走本页既有 `statusText` 提示通道（**未新造第二套 toast**）。
  新增两个纯 QML 辅助函数：`setStatus()`（写 `statusText`/`statusOk`）、`openExternal(url)`。
- `ShareDialog.qml` 结果态在复制按钮下方新增「**打开链接**」（`GhostButton`）→ `Qt.openUrlExternally(dlg.createdUrl)`；
  新增 `resultStatus` 属性 + 就地红字（打开失败提示）。创建参数 `Shares.create(fileId, code, days, maxN)` **未改**。
- 说明文案：`SharesPage` 顶部说明补两句「分享链接可直接在浏览器打开；若已设提取码，打开后需输入提取码才能下载」「提取码仅在创建时显示一次，之后无法查看，请妥善保存」；
  `ShareDialog` 结果态提示补「链接可在浏览器直接打开；若设了提取码，打开后需输入提取码才能下载」。
- 提取码明文**仅在创建时一次性返回**（`Shares.onCreated` 的 `code`）；列表项仅显示 `needCode` 徽标、**不显示明文**（本轮复核保持）。

**依赖的冻结 C++ API（已冻结、字段名不改；QML 侧 typeof 守卫）**
```qml
Shares.supported / count / busy / unsupportedNotice / refresh() /
       create(fileId, code, expireDays, maxDownloads) / revoke(shareId) / copyLink(url) / at(i)
Shares.onCreated(url, code) / onErrorOccurred(message) / onStatusMessage(message, ok)
// Shares.at(i) 键：id fileId fileName isDir url code created expire downloads maxDownloads revoked
//                   + needCode expired usable expireText remainText sizeText
```

### 8.13 控制器 `logMessage` 接线 + 文件列表「内容比对」轮询（C++ 侧，加法式）

**缺陷**：`FileController` / `AppController` / `TransferController` / `StatsController` 的 `logMessage(level, text)`
信号**全无消费者**（`TransferController` 只是把 `TransferManager::logMessage` 转发成另一个同样无人订阅的信号），
导致「列出目录失败」等关键信息**永远进不了 `cloudvault.log`**（`Logging::install()` 早已 `qInstallMessageHandler`）。

**裁决**：在装配层 `app/Application::start()` 把各控制器 `logMessage` 接到统一日志 sink
（`ERROR→qCritical`、`WARN→qWarning`、其余→`qInfo`），加来源前缀 `[Files]/[App]/[Transfer]/[Stats]/[Shares]`；每路只接一次，不重复。

**`FileController::refreshIfChanged()`（新增，加法式）**：低频轮询入口，与 `refresh()` **共用同一份**异步回包处理
（`beginLoad(pollOnly)` + `handleFolderReply()`）与**同一个 `m_refreshSeq`**：
- 失败 → 与 `refresh()` 失败路径完全一致（恰好一次 `refreshFinished(false)`）
- 成功 + 内容指纹**未变** → 不 `setItems` / 不 `setEmptyText` / **不发任何信号**（含 `refreshFinished`），仅 `logMessage(INFO,"轮询：内容未变化")`
- 成功 + 指纹**已变** → 应用列表 + 恰好一次 `refreshFinished(true)`
- `refresh()` 成功后同样更新缓存指纹 `m_lastFingerprint`（紧随的首次轮询不会误判「变了」而白刷）

### 8.14 轮询内容指纹纳入 `hash`（C++ 侧，加法式）

`FileController::fingerprintOf()` 由「`id|name|size|mtime`」改为「`id|name|size|mtime|hash`」。原因：服务端**覆盖同名文件不改
`created_at`**，仅凭前四项检不到「同 size 覆盖」；`hash`=内容 SHA-256 随内容变化（服务端列表 JSON 提供 `hash`，客户端已解析）。
`std::sort` 稳定排序与 SHA-256 汇总不变；目录 `hash` 为空串，稳定、不误报。

### 8.15 用量自动刷新 + 本机磁盘 + 下载目录 + 文件预览（C++ 侧，加法式）

（对应 §8.11 QML 侧依赖的冻结 C++ API 的实现。）

- **用量自动刷新（用户需求 item 1）**：`FileController::storageChanged()`（新增信号）在
  `createFile/createFolder/rename/remove` **成功**分支发出；`Application` 把它接到 `StatsController::refresh()`；
  上传/下载任务**成功结束后**（复用既有 `TransferManager::taskFinished`，不新造语义重复的信号）同样刷新。
- **本机磁盘**：`StatsController` 新增 `localDisks`(QVariantList) + `refreshLocalDisks()`（`QStorageInfo`，纯本机、不阻塞）。
  每项键名**冻结**：`name / label / totalText / freeText / usedText / usedRatio(0..1) / isDefault`；`isDefault` = 该卷即下载目录所在卷。
  构造即填一次；**不**放进 `refresh()`（避免每次刷服务器用量时重复枚举本机卷）。
- **下载目录生效（用户需求 item 2）**：`AppController` 新增 `downloadDir` 属性（`setDownloadDir` 只写 Settings+内存，
  `saveSettings()` 落盘并发 `downloadDirChanged`）；`TransferManager` 新增 `setDefaultDownloadDir/defaultDownloadDir`，
  下载目标目录回退链 = **显式 `destDir` > 注入的默认目录 > `AppPaths::downloadDir()`**；
  `Application::applyDownloadDirSetting()` 在启动与设置变更时注入。**§9-L4 由此消除**。
- **文件预览（用户需求 item 3，异步）**：`Backend` 新增 `getRangeAsync`（默认退化同步；`HttpBackend` 覆写为真异步，
  超时 `kPreviewTimeoutMs=15000`，**不改** `kTimeoutMs` 语义），Range 头/解析与同步 `getRange` **共用同一份**。
  `FileController` 新增只读属性 `previewMeta / previewState / previewText / previewImageUrl / previewError`、
  方法 `requestPreview(fileId)` / `clearPreview()`。`previewState` 取值**冻结**：
  `idle|loading|meta|text|image|unsupported|error`；`previewMeta` 键名**冻结**：
  `id,name,isDir,sizeText,timeText,path,hash,kind`。文本取前 64 KiB（含 NUL 或非 UTF-8 → `unsupported`），
  图片取前 4 MiB（`QImage::fromData` 成功 → `data:image/png;base64,...`，失败 → `unsupported`），其余 → `unsupported`（**仍填 `previewMeta`**）。
  独立 `m_previewSeq` 丢弃乱序回包（与 `m_refreshSeq` 分开）。

### 8.16 分享链接实装（C++ 侧，服务端已实现）

**从 §1 Unsupported 清单移除分享**。服务端契约：`POST /api/v1/shares`（body `file_id,code,expire_days,max_downloads`；
`expire_days=0` 永久、`max=0` 不限；201 回 `id,token,file_id,name,size,need_code,code(明文仅创建回显),expires_at,created_at,max_downloads,downloads,path`）、
`GET /api/v1/shares`（`{items:[...]}`，**无 `code`**）、`DELETE /api/v1/shares/:id`。
**公开端点 `GET /s/:token`（+ `/meta`）免鉴权**，由服务端提供，客户端**不调用**。

- **`Backend`** 新增异步变体 `sharesAsync / createShareAsync / revokeShareAsync`（默认退化同步；`HttpBackend` 覆写为真异步，复用 `requestAsync`）。
- **`HttpBackend`**：`shares/createShare/revokeShare` 由「不支持」桩改为真实请求；`ShareLink::url = m_baseUrl + path`（基地址与预览/下载同源）。
- **`ShareController`**（QML 上下文属性 `Shares`）：属性 `supported(=true) / unsupportedNotice(空串) / count / busy`；
  方法 `refresh() / create(fileId,code,expireDays,maxDownloads) / revoke(shareId) / copyLink(url) / at(i)`；
  信号 `sharesChanged / busyChanged / supportedChanged / created(url,code) / errorOccurred / statusMessage / logMessage`。
  构造即 `refresh()`；`create` 成功后 `emit created(url,code)` **并** `refresh()`；`revoke` 成功后 `refresh()`；`copyLink` 用 `QGuiApplication::clipboard()`。
  `at(i)` 键名**冻结**：前 11 个 `id,fileId,fileName,isDir,url,code,created,expire,downloads,maxDownloads,revoked`（与 `ShareLinkModel` 角色名一致），
  后 6 个 `needCode,expired,usable,expireText,remainText,sizeText`；`expireText`（「永久有效」/「yyyy-MM-dd HH:mm 到期」）、
  `remainText`（「不限次数」/「剩余 N 次」）、`sizeText`（`Util::humanSize`）。
- **`ShareLink`（`Types.h`）加法式**新增 `qint64 size` 与 `bool needCode`（列表不含明文 `code`，用 `need_code` 判 `needCode`）。

### 8.17 证书指纹不匹配的提示与恢复路径（QML 侧）

**背景（fail-closed）**：`HttpBackend` 对已固定（TOFU）的主机校验服务端证书指纹；**不匹配时直接拒绝连接、不触发 `ignoreSslErrors`、不发起任何请求**，并发出信号
`certPinMismatch(hostPort, expectedFingerprint, actualFingerprint, subject, issuer, validity)`（`App` 上下文属性转发）。这是**安全默认**：指纹变化可能是服务器换证，也可能是中间人攻击，因此**绝不静默信任**。

**新增对话框 `qml/dialogs/CertMismatchDialog.qml`**（已登记进 `resources/resources.qrc`）：

- 入参方法 `openFor(hostPort, expectedFingerprint, actualFingerprint, subject, issuer, validity)`；六项均为字符串（缺失传空串）。
- 内容：醒目「已拒绝连接（服务器证书已变更）」标题 → **新旧指纹并排对比**（等宽字体、`TextEdit.WrapAnywhere` 可换行、每块独立「复制」按钮）→ `openssl` 手工核对提示 → `subject / issuer / validity` 明细 → 安全提示（**不要**在无法解释变化时继续）→ 状态行。
- 三个动作（**默认不信任**）：
  1. **取消（保持拒绝）**——`SecondaryButton`，关闭对话框，不改动任何持久化状态（保持 fail-closed）。
  2. **清除记录并重新信任**——`DangerButton`；点击后**先** `typeof App.clearPinnedFingerprint === "function"` 守卫（未落地则不误报并提示"当前版本不支持"），**通过后**调用 `App.clearPinnedFingerprint()` 清除该主机旧指纹记录，随后 `close()` 并 `emit clearAndRetryRequested()`。**只有用户显式点击才清除**。
  3. **去设置页**——`GhostButton`，`emit openSettingsRequested()`（用于查看/管理固定指纹）。
- **安全约束（冻结）**：对话框**不提供**「仍然信任」类一键放行；`clearPinnedFingerprint()` **仅在用户显式点击「清除记录并重新信任」时**被调用——**绝不自动信任**。

**`qml/Main.qml` 接线**：

- `CertMismatchDialog { id: certMismatchDialog }`；`onClearAndRetryRequested`：清 `certMismatchSeen[hostPort]`、Toast「已清除该主机指纹记录，正在重新连接…」、`filesPage.refreshNow()`；`onOpenSettingsRequested`：`window.navIndex = window.pageSettings`。
- `Connections { target: (typeof App !== "undefined") ? App : null; ignoreUnknownSignals: true; function onCertPinMismatch(...) }`
  —— `ignoreUnknownSignals: true` + `typeof App` 守卫，使 C++ 信号尚未落地时不报错；按 `hostPort` 去重（`property var certMismatchSeen: ({})`），**同一主机只弹一次**，避免重连风暴反复打扰。

---

## 9. 已知限制（v1，经独立 QA 验证后确认，非缺陷但需知悉）

| # | 限制 | 影响 | 后续建议 |
|---|---|---|---|
| L1 | **部分**控制器网络调用仍在主线程同步执行（`FileController::rename/remove/createFolder`、`AppController::healthCheck/refreshStorage` 仍直接调用阻塞式 `Backend`，超时 60s）。**启动/自动刷新路径已异步化**（§8.10/§8.13/§8.15） | 服务器不可达时上述**手动操作**最长冻结 60s（启动已无此问题） | 后续按同一异步模式改造 |
| L2 | 传输队列的 401 文案已映射为可操作提示，但 `TransferManager` 内部仍原样透出 `HTTP xxx` | 影响文案一致性（已由 `TransferController` 兜住） | 后续在 net 层统一错误语义 |
| L3 | `FileListModel.status`（秒传标记）恒为 `-`（服务端列表接口不返回 instant 字段），QML 的秒传徽标为**死代码** | 列显示无意义 | 要么由 manifest 回填，要么从界面移除该列 |
| L4 | ~~下载目录不一致~~ **已解决（§8.15）**：`TransferManager` 目标目录回退链 `显式 destDir > Settings::downloadDir() 注入值 > AppPaths::downloadDir()`；`AppController::downloadDir` 暴露给 QML 并持久化 | — | 已消除 |
| L5 | 本地下载收尾是"先删后改名"（非原子） | 改名失败会丢本地同名旧文件（可重下，低危） | 改为 `QFile::rename` 覆盖语义或先备份 |
| L6 | 服务端 `/api/v1/files` 单次上限 500 条（无分页） | 文件数 >500 时列表不全 | 服务端加分页（独立任务） |
| L7 | 深色模式未启用（`qtquickcontrols2.conf` 固定 Light） | 无法跟随系统深色 | 切换 conf + `Theme.darkMode` 即可（令牌已就位） |
| L8 | 冗余 API（QML 未使用）：`Files.emptyText/supported`、`Stats.freeBytes/totalBytes/supported`、`TransferController.supported`、`Auth/Sync` 全部属性、`Core` 上下文属性 | 维护噪声 | 后续收敛 |
